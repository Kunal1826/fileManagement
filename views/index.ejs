<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
<div class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold text-gray-800 mb-4">File Management System</h1>
    <div class="flex flex-wrap justify-between">
        <div class="w-1/2 p-4">
            <h2 class="text-2xl font-semibold text-gray-700 mb-10">File Management</h2>
            <form action="/createfile" method="get">
                <div class="mb-2">
                    <label for="filename" class="block text-sm font-medium text-gray-700">Filename</label>
                    <input type="text" name="filename" id="filename" class="mt-1 block rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm w-[200px]" required>
                </div>
                <div class="mb-2">
                    <label for="content" class="block text-sm font-medium text-gray-700">Content</label>
                    <textarea name="content" id="content" rows="3" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" required></textarea>
                </div>
                <button type="submit" class="inline-flex items-center px-3 py-2 border border-transparent text-sm leading-4 font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">Create File</button>
            </form>
        </div>
        <div class="w-1/2 p-4">
            <h2 class="text-2xl font-semibold text-gray-700 mb-2">View Files</h2>
            <div class="flex flex-wrap justify-between w-full">
             <%  files.forEach(function(file){ %>
                <div class="w-1/2 p-4">
                    <div class="bg-white shadow-lg rounded-lg p-4">
                        <h2 class="text-2xl font-semibold text-gray-700 mb-2"><%= file.name %></h2>
                        <div class="flex justify-between">
                            <a href="viewfile/<%= file.name%>" class="border border-blue-500 text-blue-500 px-4 py-2 rounded">View</a>
                            <a href="Deletefile/<%= file.name%>" class="ml-4 border border-red-500 text-red-500 px-4 py-2 rounded">Delete</a>
                            <a href="Editfile/<%= file.name%>" class="ml-4 border border-black text-black px-4 py-2 rounded">Edit</a>
                        </div>
                    </div>
                </div>
               <% }) %>
            
            </div>
        </div>
    </div>
</div>
</body>
</html>